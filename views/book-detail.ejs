<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Exo+2:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- Animated Background -->
    <div class="bg-animation">
        <div class="star-field">
            <% for(let i = 0; i < 100; i++) { %>
                <div class="star" style="left: <%= Math.floor(Math.random() * 100) %>%; top: <%= Math.floor(Math.random() * 100) %>%; animation-delay: <%= (Math.random() * 3).toFixed(1) %>s;"></div>
            <% } %>
        </div>
        <div class="nebula nebula-1"></div>
        <div class="nebula nebula-2"></div>
        <div class="nebula nebula-3"></div>
    </div>

    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">
                <i class="fas fa-atom"></i>
                <span>BookVerse</span>
            </a>
            
            <div class="nav-menu" id="nav-menu">
                <a href="/" class="nav-link">
                    <i class="fas fa-home"></i>
                    <span>Home</span>
                </a>
                <a href="/books" class="nav-link">
                    <i class="fas fa-book"></i>
                    <span>Explore</span>
                </a>
                <a href="/ai-finder" class="nav-link ai-link">
                    <i class="fas fa-robot"></i>
                    <span>AI Finder</span>
                </a>
                
                <% if (user) { %>
                    <a href="/profile" class="nav-link">
                        <i class="fas fa-user"></i>
                        <span>Profile</span>
                    </a>
                    <form action="/logout" method="POST" style="display: inline;">
                        <button type="submit" class="nav-link logout-btn">
                            <i class="fas fa-sign-out-alt"></i>
                            <span>Logout</span>
                        </button>
                    </form>
                <% } else { %>
                    <a href="/login" class="nav-link">
                        <i class="fas fa-sign-in-alt"></i>
                        <span>Login</span>
                    </a>
                    <a href="/register" class="nav-link register-btn">
                        <i class="fas fa-user-plus"></i>
                        <span>Register</span>
                    </a>
                <% } %>
            </div>
            
            <div class="nav-toggle" id="nav-toggle">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            <!-- Book Detail -->
            <div style="display: grid; grid-template-columns: 400px 1fr; gap: 3rem; margin: 2rem 0; background: var(--card-bg); padding: 3rem; border-radius: var(--radius-2xl); border: 1px solid rgba(0, 212, 255, 0.2); backdrop-filter: blur(20px);">
                <div>
                    <img src="<%= book.coverImage %>" alt="<%= book.title %>" style="width: 100%; border-radius: var(--radius-xl); box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);">
                </div>
                
                <div>
                    <h1 style="font-family: 'Orbitron', monospace; color: var(--text-primary); margin-bottom: 1rem; font-size: 2.5rem; font-weight: 700; line-height: 1.2;">
                        <%= book.title %>
                    </h1>
                    <h2 style="color: var(--text-secondary); margin-bottom: 2rem; font-size: 1.4rem; font-weight: 500;">
                        by <%= book.author %>
                    </h2>
                    
                    <div style="margin-bottom: 2rem; padding: 2rem; background: var(--secondary-bg); border-radius: var(--radius-xl); border: 1px solid rgba(0, 212, 255, 0.1);">
                        <div style="margin-bottom: 1rem; color: var(--text-secondary);">
                            <strong style="color: var(--text-primary);">Published:</strong> <%= book.publishedYear %>
                        </div>
                        <% if (book.isbn) { %>
                            <div style="margin-bottom: 1rem; color: var(--text-secondary);">
                                <strong style="color: var(--text-primary);">ISBN:</strong> <%= book.isbn %>
                            </div>
                        <% } %>
                        <div style="margin-bottom: 1rem; color: var(--text-secondary);">
                            <strong style="color: var(--text-primary);">Pages:</strong> <%= book.pages %>
                        </div>
                        <div style="margin-bottom: 1rem; color: var(--text-secondary);">
                            <strong style="color: var(--text-primary);">Publisher:</strong> <%= book.publisher %>
                        </div>
                        <div style="margin-top: 1.5rem;">
                            <strong style="color: var(--text-primary); display: block; margin-bottom: 0.5rem;">Genres:</strong>
                            <div>
                                <% book.genre.forEach(g => { %>
                                    <span class="genre-tag" style="margin-right: 0.5rem; margin-bottom: 0.5rem;"><%= g %></span>
                                <% }) %>
                            </div>
                        </div>
                    </div>
                    
                    <div style="margin-bottom: 2rem; padding: 2rem; background: var(--secondary-bg); border-radius: var(--radius-xl); border: 1px solid rgba(0, 212, 255, 0.1);">
                        <div style="font-size: 1.3rem; margin-bottom: 1.5rem; color: var(--neon-orange); font-weight: 700; display: flex; align-items: center; gap: 0.5rem;">
                            <i class="fas fa-star"></i>
                            <span><%= book.averageRating %></span>
                            <span style="font-size: 1rem; font-weight: 500; opacity: 0.8;">
                                (<%= book.ratingsCount %> ratings)
                            </span>
                        </div>
                        
                        <% if (user) { %>
                            <div>
                                <p style="margin-bottom: 1rem; font-weight: 600; color: var(--text-primary);">Rate this book:</p>
                                <div style="display: flex; gap: 0.5rem;">
                                    <% for (let i = 1; i <= 5; i++) { %>
                                        <button class="rating-star" data-rating="<%= i %>" data-book-id="<%= book._id %>" style="background: none; border: none; font-size: 2rem; cursor: pointer; color: rgba(255, 133, 0, 0.3); transition: all 0.2s ease;">
                                            <i class="fas fa-star"></i>
                                        </button>
                                    <% } %>
                                </div>
                            </div>
                        <% } %>
                    </div>
                    
                    <% if (book.description) { %>
                        <div style="margin-top: 2rem;">
                            <h3 style="color: var(--text-primary); margin-bottom: 1rem; font-size: 1.5rem; font-weight: 600;">
                                About This Book
                            </h3>
                            <p style="line-height: 1.8; color: var(--text-secondary); font-size: 1.1rem;">
                                <%= book.description %>
                            </p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Related Books -->
            <% if (relatedBooks.length > 0) { %>
                <div style="margin-top: 4rem;">
                    <h2 style="text-align: center; font-family: 'Orbitron', monospace; font-size: 2.5rem; margin-bottom: 2rem; background: linear-gradient(135deg, #00d4ff 0%, #8b5cf6 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                        Related Books
                    </h2>
                    
                    <div class="books-grid">
                        <% relatedBooks.forEach(book => { %>
                            <div class="book-card">
                                <a href="/books/<%= book._id %>" style="text-decoration: none; color: inherit;">
                                    <img src="<%= book.coverImage %>" alt="<%= book.title %>" class="book-cover">
                                    <div class="book-info">
                                        <h3 class="book-title"><%= book.title %></h3>
                                        <p class="book-author">by <%= book.author %></p>
                                        <div class="book-genres">
                                            <% book.genre.slice(0, 2).forEach(g => { %>
                                                <span class="genre-tag"><%= g %></span>
                                            <% }) %>
                                        </div>
                                        <div class="book-rating">
                                            <i class="fas fa-star"></i>
                                            <span><%= book.averageRating %></span>
                                            <span style="color: var(--text-muted);">(<%= book.ratingsCount %>)</span>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        <% }) %>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3><i class="fas fa-atom"></i> BookVerse</h3>
                    <p>Discover your next great read with AI-powered recommendations in the digital age.</p>
                </div>
                <div class="footer-section">
                    <h4>Explore</h4>
                    <ul>
                        <li><a href="/books">All Books</a></li>
                        <li><a href="/ai-finder">AI Finder</a></li>
                        <li><a href="/books?genre=Science Fiction">Sci-Fi</a></li>
                        <li><a href="/books?genre=Fantasy">Fantasy</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h4>Connect</h4>
                    <div class="social-links">
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-facebook"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-github"></i></a>
                    </div>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 BookVerse. All rights reserved. | Powered by AI</p>
            </div>
        </div>
    </footer>

    <script src="/js/main.js"></script>
    <style>
        @media (max-width: 768px) {
            .container > div:first-child {
                grid-template-columns: 1fr !important;
                gap: 2rem !important;
            }
        }
    </style>
</body>
</html>